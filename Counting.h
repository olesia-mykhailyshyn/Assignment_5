#ifndef COUNTING_H
#define COUNTING_H

#include <string>
#include <vector>

using namespace std;

class Counting {
public:
    static string countingPostfix(const vector<string>& notation) ;
};

#endif